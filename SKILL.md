---
name: a3-report
description: 基于丰田A3方法论生成结构化A3报告并支持教练式引导，适用于所有行业（IT/服务/交付/运维/医疗/项目管理/制造等）。支持问题解决型（7分区+5Why）、提案型、状态汇报型；可输出Markdown、HTML单页A3版式、PPTX、Excel；支持"A3教练模式"用提问引导用户完成分析。当用户要求写A3报告、根因分析、改善提案、复盘提效报告、精益问题解决，或提到"A3"、"5Why"、"PDCA"、"A3教练"时使用。
argument-hint: <问题描述或报告主题>
---

# A3 报告生成器

## Overview

按丰田A3方法论，将问题分析/提案/项目状态浓缩为一页结构化报告。A3不仅是报告工具，更是沟通与辅导工具：信息充足时直接生成；信息不足时以教练式提问引导用户完成A3思考。适用于任何行业，不预设制造业场景。

**角色分工（必须遵守）**：本技能是一套方法论，不替用户思考。用户负责提供数据、事实和对自己问题的思考；AI 负责用提问引导思考路径、把关方法论质量（根因是否挖到位、目标是否可量化、计划是否可落地）、并输出专业的结果物。禁止替用户臆造数据和结论。

## 工作流程

### 第0步：首次使用引导（本次会话第一次触发本技能时必做）

在开始任何提问或生成之前，先用 5-8 行向用户说明本技能的用法，让用户知道会发生什么、自己需要做什么。要点：

1. **这是什么**：一套丰田A3问题解决方法论，最终产出一页结构化报告（Markdown/HTML/PPTX/Excel 按需选择）。
2. **你的角色**：提供真实数据和你对问题的思考——数据和答案在你那里，我只负责引导和成稿。
3. **我的角色**：像A3教练一样追问（尤其是5Why根因追问，会连续问"为什么"直到挖到根因，请耐心配合）；把目标做成 OKR 结构、把计划按 SMART 原则打磨。
4. **流程预告**：确认问题（背景+现状数据）→ 分析（目标→5Why根因→对策）→ 落地（SMART计划→跟踪），每阶段与你共识后再进入下一阶段。
5. **诚实原则**：没有的数据我会标 `[待确认]`，不会编造；缺关键信息时我会追问而不是猜。

说明后再进入第1步。若用户明确表示"直接生成、别引导"，压缩为2行提示后继续。

### 第1步：确定A3类型与模式

**类型**（根据场景自动判断，无法判断时用一个问题确认）：

| 类型 | 适用场景 | 关键词信号 |
|---|---|---|
| 问题解决型 (problem-solving) | 问题/低效/超支需根因分析改善 | 问题、故障、投诉、提效、复盘、根因、超支 |
| 提案型 (proposal) | 新方案、投资、变更需要决策审批 | 提案、方案、建议、审批、立项 |
| 状态汇报型 (status) | 项目/改善活动的进展汇报 | 进展、汇报、阶段总结 |

**模式**：

- **直接生成**：用户提供了足够信息（见类型必填清单），直接产出报告
- **教练模式**：用户信息不足、或明确要求引导时，读取 `references/a3-coaching.md`，按五步提问流程（理清问题→影响→目标→障碍/根因→行动）引导。遵循教练心法：不直接给答案、先肯定再引导、一次提问≤4个（用 AskUserQuestion 批量提问）

### 第2步：信息盘点与补齐

读取 `references/a3-types.md` 中对应类型的分区结构和必填信息清单。

- 对照清单检查用户已提供的信息；关键信息缺失时按教练模式提问补齐。
- 建议按**三阶段对话**推进：①确认问题（背景+现状） ②分析（目标→根因→对策） ③落地（计划→跟踪），每阶段与用户共识后再继续。用户要求一次性生成时可跳过。
- 次要信息缺失用 `[待补充：责任人]` 占位并在交付时提醒。
- 不要臆造数据。用户没给的数字一律标注 `[待确认]`。
- 引导用户用现状分析方法补强证据：进现场调研多方角色、工序拆解找无效投入、对标做得好的案例、数据对比暴露差距（详见 a3-types.md）。

### 第3步：生成 Markdown 报告（主内容）

Markdown 是内容的唯一权威来源，其他格式由它派生。要求：

- 按 `references/a3-types.md` 中该类型的分区顺序组织二级标题（问题解决型为7分区：背景/目的→现状→目标/课题→根因分析→解决方案→执行计划→后续跟踪）。
- **叙事性**：A3是讲一个连贯的故事，不是填格子——每个分区回答上一分区引出的疑问（背景引出"现状多严重"→现状引出"要到哪去"→目标引出"是什么挡住了"→根因引出"怎么办"…），任何读者5分钟内能顺着读懂全篇逻辑。
- **问题陈述=差距**：现状区的问题陈述只描述"标准/期望与现状之间的差距"（量化），不得夹带原因（"因为…"）或对策（"需要…"）。
- 标题区包含：报告标题、类型、实践人（作者）、A3教练（如有）、日期、版本。
- 每区精炼（3-8行要点），以数据和事实为主；重点结论加粗。
- **5Why根因追问是硬性要求**：不能由 AI 自己补全 Why 链。每个问题现象都要向用户逐层追问"为什么"，用户给出的每层原因先复述确认、再问下一层；遇到"等待/延误/能力弱/不重视"等线索词必须继续追问；直到满足根因判定标准（消除该原因即可根治、防止再发）且用户确认为止。追问协议详见 `references/a3-coaching.md` 的"5Why追问协议"。报告中用5Why表格展示完整追问链（问题现象→Why1→…→根因→改善举措），区分"线索/现象"与"根因"。
- **目标区用 OKR 结构**：1个 O（定性方向，回答"解决后理想状态是什么"）+ 2-4个 KR（可量化关键结果：指标+当前值→目标值+期限）。KR 同时满足目标三要素：有依据、可量化、有价值；KR 即监控指标。
- 对策与根因一一对应，且是具体可落地的动作（不是"加强""提升"类理念）；解决方案区展示多方案比选。
- **执行计划每行按 SMART 校验**：行动具体（S，在哪个节点做什么、输出什么交付物）、可衡量（M，完成标准/验收物）、可达成（A，责任人有资源和权限）、与根因/KR相关（R，标注对应根因或KR编号）、有期限（T，明确完成日期）。表格列：行动/交付物/责任人/完成时间/对应根因或KR。任一行不满足 SMART，先向用户追问补齐再写入。

### 第4步：按需生成其他格式

先将报告内容整理为 JSON（结构见下），保存为 `a3.json`：

```json
{
  "type": "problem-solving",
  "title": "报告标题",
  "meta": {"author": "实践人", "coach": "教练", "date": "2026-07-31", "department": "部门", "version": "v1.0"},
  "sections": [
    {"title": "1. 背景/目的", "content": "多行文本\n用换行分隔要点", "side": "left"},
    {"title": "5. 解决方案（对策）", "content": "...", "side": "right"}
  ]
}
```

`side` 按类型分区表中的左/右栏归属填写。

**HTML 交互式报告**：以 `assets/a3-template.html` 为模板复制修改。模板为**屏幕优先的交互式设计**（sticky导航自动生成、分区折叠、A-/A+字号调节、回到顶部），打印时自动切回单页A3横向版式。结构约定：

- 替换标题区 `{{...}}` 占位符；左栏分区放入 `<div class="col left">`、右栏放入 `<div class="col right">`
- 每个分区：`<section class="box" id="sec-N"><h2><span class="num">N</span>标题<span class="chev">▼</span></h2><div class="body">内容</div></section>`（id必须为 sec-N，导航自动生成）
- 表格外层包 `<div class="table-wrap">`；单区内容极长时给 body 加 `scroll` 类（内部滚动，避免页面被拉长）
- 可用组件：`.kpi-row/.kpi`（目标大数字卡）、`.tag-ok/.tag-try/.tag-no`（对策结论胶囊）、`.why-chain` + `<span class="sep">→</span>`（5Why箭头链）、`.placeholder`（待确认高亮）
- 保持 CSS 与 `<script>` 不动

**PPTX / Excel**：运行脚本：

```bash
python <skill-dir>/scripts/a3_build.py a3.json --pptx A3报告.pptx --xlsx A3报告.xlsx
```

可只传 `--pptx` 或 `--xlsx`。脚本生成单页A3横向幻灯片和双栏布局工作表。

### 第5步：质量自检

交付前按 `references/a3-types.md` 末尾的质量检查清单逐项核对，重点防范常见误区：

- 问题与对策是否分开（问题是"为什么发生"，不是"如何解决"）
- 是否把线索当根因（等待/延误/能力弱要继续追问为什么）；5Why链是否由用户逐层确认而非AI代填
- 对策是否具体可落地（执行方知道在哪个节点做什么、输出什么）
- 目标是否为 OKR 结构（O+2-4个KR），KR是否有当前值→目标值+期限
- 执行计划是否每行通过 SMART 校验（交付物/完成标准/责任人/期限/对应根因或KR）
- 是否存在臆造数据（应为待确认标注）
- 全篇是否连成一个故事：从背景顺读到跟踪，逻辑无断层、无跳跃

**交付时向用户说明两点**：①A3是活文档——对策执行和效果确认后应回来更新报告并升版本号（PDCA循环），未达标的KR要做偏差分析；②建议拿着报告与相关方做"根回"（nemawashi）：正式评审前逐一与利益相关者沟通、吸收意见修订，达成共识后再定稿执行。

## Resources

- `references/a3-types.md` — 三种A3类型的分区结构、5Why表格格式、OKR目标写法、SMART计划校验表、现状分析方法、必填清单、质量检查清单
- `references/a3-coaching.md` — 教练模式：五步提问流程、5Why追问协议、书写十问、三阶段对话、常见误区应对
- `assets/a3-template.html` — A3横向单页可打印HTML模板
- `scripts/a3_build.py` — JSON → PPTX/XLSX 生成脚本（依赖 python-pptx、openpyxl）
